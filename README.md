Commands we used 
like 

git init
git remote add origin <origin>
git add .
git commit -m "msg"
git branch -M  main
git push -u origin main

git add README.md
 changed with apply ruleset on main branch 

 ....